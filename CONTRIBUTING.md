# Contributing

Do not commit images, labels, weights, checkpoints, model predictions, or any
machine-specific absolute path. Keep all new commands relative to the repository
root and document the seed, split, model version, and selection metric for each
experiment. Run Python syntax checks before opening a pull request.
