# Supervised Private Data

This directory is populated by `prepare_dataset.py` and intentionally contains
no images or labels in the public repository. See the root README for the expected
numbered-folder source layout and preparation command.
