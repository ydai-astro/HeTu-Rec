# Private Data Directory

This directory is a local placeholder only. Do not commit images, labels, FITS
files, or derived catalogs.

```text
data/
└── raw_images/             # PNG files for unsupervised feature extraction

supervised/Data/
├── images/                 # Generated labeled images
├── splits/                 # Generated train/val/test CSVs
└── rest/                   # Optional unlabeled PNGs for post-test inference
```
